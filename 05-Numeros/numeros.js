let altura_cm = 184;

let altura_m = 1.84;

let peso_kg = 85.5;

let altura_round = Math.ceil(altura_m);
console.log(altura_round);

let peso_round = Math.floor(peso_kg);
console.log(peso_round);

let iguales = Number.MAX_VALUE + 1 === Number.MAX_VALUE;
